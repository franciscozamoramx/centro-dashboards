name: Custom Deployment

on:
  # ✅ Esto SÍ te dará botón "Run workflow"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        default: 'production'
        type: choice
        options:
        - production
        - staging
  
  # También se ejecuta automáticamente en push
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Deploy to GitHub Pages
      run: |
        echo "🚀 Desplegando a GitHub Pages..."
        echo "Esta es una implementación personalizada"
        echo "Puedes agregar aquí tus propios pasos"
        echo "✅ Despliegue completado"
    
    - name: Notify deployment
      run: |
        echo "📢 Notificación: Despliegue exitoso"
        echo "Fecha: $(date)"
        echo "Usuario: ${{ github.actor }}"
